# Infra - Deployment Configuration
# NOTE: webhook is excluded — it cannot self-deploy (kills itself mid-process)
# To update webhook-server: ssh prodesk "cd ~/Hosting/Infra && docker compose up -d --build webhook"
name: Infra
path: Infra
branch:
  - master
  - main
repos:
  - Infra
services:
  - homarr
  - pihole
