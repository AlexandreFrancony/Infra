# Infra - Deployment Configuration
# Only rebuild webhook service, not postgres (avoids killing the database)
name: Infra
path: Infra
branch:
  - master
  - main
repos:
  - Infra
services:
  - webhook
